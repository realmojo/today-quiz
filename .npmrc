# create-next-app이 pnpm-workspace.yaml(allowBuilds용)을 만들면서 이 프로젝트가
# 워크스페이스 루트로 잡힌다. 그래서 pnpm add가 ERR_PNPM_ADDING_TO_ROOT로 막힌다.
# 패키지가 하나뿐인 저장소라 루트에 넣는 것이 맞으므로 그 확인을 끈다.
ignore-workspace-root-check=true
